# ============================================
# DeepTutor Server — Full Dependencies (CLI + Web)
# ============================================
# Install: pip install -r requirements/server.txt && pip install -e .
# Everything needed to run the API server and web frontend.

-r cli.txt

# --- API server ---
fastapi>=0.100.0
uvicorn[standard]>=0.24.0
websockets>=12.0
python-multipart>=0.0.6

# --- Meridian SaaS layer (auth, tenancy, persistence) ---
# Not otherwise pulled in by requirements.txt's Docker build chain, which
# would otherwise leave meridian/ unable to import in production.
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0
alembic>=1.13.0
python-jose[cryptography]>=3.3.0
# passlib 1.7.4 (its last release) probes bcrypt.__about__.__version__,
# which bcrypt>=4.1 removed; pin below that to avoid a ValueError at first
# hash/verify call. See meridian/platform/auth/passwords.py.
passlib[bcrypt]>=1.7.4
bcrypt<4.1
email-validator>=2.1.0
